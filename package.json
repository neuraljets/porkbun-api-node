{
  "name": "porkbun-api-node",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "build": "tsc",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "gts": "^5.3.0",
    "typescript": "~5.4.3"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "axios-retry": "^4.4.1",
    "bottleneck": "^2.19.5"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
